# ═══════════════════════════════════════════
# Media Stack VPS — Variables d'environnement
# ═══════════════════════════════════════════
# Copier ce fichier en .env et remplacer toutes les valeurs CHANGE_ME

# Système
PUID=1000
PGID=1000
TZ=Europe/Paris

# Chemins sur le VPS
DOWNLOADS_PATH=/data/downloads
MEDIA_PATH=/data/media

# ── VPN Mullvad WireGuard ──────────────────
# Générer sur https://mullvad.net/fr/account/wireguard-config
WIREGUARD_PRIVATE_KEY=CHANGE_ME
WIREGUARD_ADDRESSES=CHANGE_ME

# ── Freebox SSH (pour rclone) ──────────────
FREEBOX_HOST=CHANGE_ME
FREEBOX_USER=CHANGE_ME
FREEBOX_MEDIA_PATH=/mnt/NVMe/media

# ── Domaine (pour Caddy reverse proxy) ────
# Doit pointer vers l'IP du VPS (DNS A record)
DOMAIN=CHANGE_ME

# ── Caddy Basic Auth ──────────────────────
# Générer le hash : docker run --rm caddy caddy hash-password
CADDY_USER=CHANGE_ME
CADDY_PASSWORD_HASH=CHANGE_ME

# ── SSH (pour harden.sh) ─────────────────
SSH_PORT=2222

# ── Port qBittorrent (via Gluetun) ──────
PORT_QBITTORRENT=8080

# ── Webhook (optionnel) ──────────────────
# URL Discord/Slack pour notifications de sync et Watchtower
WEBHOOK_URL=
# Décommenter pour activer les notifications Watchtower via Slack
# WATCHTOWER_NOTIFICATIONS=slack
